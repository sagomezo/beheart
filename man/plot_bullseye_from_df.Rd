% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bullseye.R
\name{plot_bullseye_from_df}
\alias{plot_bullseye_from_df}
\title{Create a Bull's Eye Plot from a Data Frame}
\usage{
plot_bullseye_from_df(
  df,
  model = 17,
  group_col,
  value_col,
  summary_fun = "mean_sd",
  facet_by = NULL,
  facet_labels = NULL,
  text_display = "value",
  palette = "default",
  value_name = NULL,
  show_anatomical_labels = TRUE,
  line_color = "white",
  font_family = "sans",
  font_color_text = "black",
  font_size_text = 9,
  font_color_labels = "black",
  font_size_labels = 4,
  filename = NULL,
  width = 7,
  height = 7,
  units = "in",
  dpi = 300
)
}
\arguments{
\item{df}{The input data frame.}

\item{model}{An integer, 16 or 17, for the segmentation model.}

\item{group_col}{The unquoted name of the column identifying the segments.}

\item{value_col}{The unquoted name of the column containing the values to plot.}

\item{summary_fun}{The summary statistic to display: "mean_sd" or "median_iqr".}

\item{facet_by}{Optional unquoted name of a column to create side-by-side plots.}

\item{facet_labels}{Optional named vector for custom facet labels.}

\item{text_display}{What to display in segments: "value", "name", or "none".}

\item{palette}{A built-in palette ("default", "echo", "colorblind").}

\item{value_name}{A string for the legend's title. If NULL, a default is used.}

\item{show_anatomical_labels}{A boolean; if TRUE, labels for "Anterior", etc. are shown.}

\item{line_color}{The color of the lines separating segments.}

\item{font_family}{The font family for all text.}

\item{font_color_text}{The color of the text inside segments.}

\item{font_size_text}{The font size (in points) of the text inside segments.}

\item{font_color_labels}{The color of the peripheral anatomical labels.}

\item{font_size_labels}{The font size of the peripheral anatomical labels.}

\item{filename}{Optional path to save the plot (e.g., "my_plot.png").}

\item{width}{The width of the saved plot. Defaults to 7.}

\item{height}{The height of the saved plot. Defaults to 7.}

\item{units}{The units for width and height ("in", "cm", "mm"). Defaults to "in".}

\item{dpi}{The resolution for saved raster images. Defaults to 300.}
}
\value{
A ggplot object.
}
\description{
This is the main plotting engine for the beheart package. It creates a
bull's eye plot by calculating summary statistics from a data frame. It supports
extensive customization, faceting for group comparisons, and direct export.
}
\examples{
# --- Create a sample long-format data frame ---
# This is the format required by the function. Use `prepare_bullseye_data`
# if your data is in a "wide" format.
set.seed(123)
long_dataset <- data.frame(
  segment = rep(1:17, each = 20),
  strain = rnorm(340, mean = -18, sd = 5),
  hypertension = rep(c("Yes", "No"), each = 10, times = 17)
)
long_dataset$strain[long_dataset$hypertension == "Yes"] <-
  long_dataset$strain[long_dataset$hypertension == "Yes"] + 3

# --- Example 1: A simple, default plot ---
plot_bullseye_from_df(
  df = long_dataset,
  group_col = segment,
  value_col = strain
)

# --- Example 2: Faceted plot with custom labels and export ---
\dontrun{
plot_bullseye_from_df(
  df = long_dataset,
  group_col = segment,
  value_col = strain,
  facet_by = hypertension,
  facet_labels = c("Yes" = "Hypertensive", "No" = "Normotensive"),
  palette = "echo",
  value_name = "Peak Strain (\%)",
  filename = "hypertension_comparison.png",
  width = 11, height = 6
)
}

# --- Example 3: Displaying segment names instead of values ---
plot_bullseye_from_df(
  df = long_dataset,
  group_col = segment,
  value_col = strain,
  text_display = "name"
)
}
